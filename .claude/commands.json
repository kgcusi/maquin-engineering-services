{
  "typecheck": "pnpm typecheck",
  "lint": "pnpm lint",
  "format": "pnpm format",
  "build": "pnpm build",
  "dev": "pnpm dev",
  "db:generate": "pnpm db:generate",
  "db:migrate": "pnpm db:migrate"
}
